# Agentic-PDF-Processor-Gemini-FastAPI
This project ingests PDFs, builds a vector DB of text chunks, and exposes an agentic workflow using Google's Gemini models (via `google-generativeai`) to:  - Summarize the document - Classify into labels - Extract citation-like references - Answer context-aware questions (RAG)
